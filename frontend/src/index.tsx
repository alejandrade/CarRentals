import React from 'react';
import ReactDOM from 'react-dom';
import "./index.css";

const App: React.FC = () => <h1>Hello, React with TypeScript changed!</h1>;

ReactDOM.render(<App />, document.getElementById('root'));
